const PopularPostStyle = (theme) => ({
    title: {
        backgroundImage: `linear-gradient(transparent 90%,${theme.palette.primaryOrange.main} 50%)`,
        width: 'fit-content',
        paddingBottom: 20,
    },
});

export default PopularPostStyle;
